# Lambda Without Parameters

##### Always Multiline
```kotlin
button.onClick {
    oneAction()
}

button.onClick {
    firstAction()
    secondAction()
}
```

##### Singleline for one action
```kotlin
button.onClick { oneAction() }

button.onClick {
    firstAction()
    secondAction()
}
```
